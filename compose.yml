name: heymimic
services:
  postgres:
    image: postgres:17.6-alpine
    environment:
      POSTGRES_DB: heymimic
      POSTGRES_USER: heymimic
      POSTGRES_PASSWORD: heymimic_dev
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U heymimic -d heymimic"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes: ["heymimic-postgres:/var/lib/postgresql/data"]
volumes:
  heymimic-postgres:

